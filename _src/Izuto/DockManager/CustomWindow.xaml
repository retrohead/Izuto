<Window x:Class="Izuto.Controls.CustomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:chrome="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:controls="clr-namespace:Izuto.Controls"
        Title="DockableWindow"
        Height="450" Width="800"
        WindowStyle="None"
        ResizeMode="CanResize"
        AllowsTransparency="True"
        Background="Transparent" MinWidth="260" SourceInitialized="Window_SourceInitialized" Deactivated="Window_Deactivated" Activated="Window_Activated" StateChanged="Window_StateChanged" SizeChanged="Window_SizeChanged">

    <chrome:WindowChrome.WindowChrome>
        <chrome:WindowChrome
            CaptionHeight="0"
            ResizeBorderThickness="8"
            GlassFrameThickness="0"
            UseAeroCaptionButtons="False"/>
    </chrome:WindowChrome.WindowChrome>

    <!-- draw your own chrome inside -->
    <Border x:Name="mainBorder">
        <Grid x:Name="mainGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
                <RowDefinition Height="100*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <controls:CustomTitleBar x:Name="customTitleBar" Icon="/Resources/Izuto.ico" FontSize="8" Height="32" Foreground="{DynamicResource HeaderTextBrush}" VerticalAlignment="Top" TargetWindow="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
            <Border Grid.Row="1" Margin="0,0,0,0"
                    Background="{Binding Background, RelativeSource={RelativeSource AncestorType=Window}}" x:Name="contentArea" HorizontalAlignment="Stretch">
            </Border>
        </Grid>
    </Border>
</Window>
